# Production Environment Variables
# Copy these to your hosting platform's environment variable settings

# Server Configuration
PORT=5000
NODE_ENV=production

# Database Configuration (PostgreSQL)
# These will be provided by your hosting service (Render/Railway)
DATABASE_URL=postgresql://user:password@host:5432/database
# OR individual variables:
DB_HOST=your-db-host.region.provider.com
DB_USER=your_db_user
DB_PASSWORD=your_secure_db_password
DB_NAME=american_bank_united
DB_PORT=5432

# JWT Secret - GENERATE A STRONG SECRET!
# Use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=generate_a_strong_random_secret_at_least_32_characters_long
JWT_EXPIRES_IN=24h

# Security
BCRYPT_ROUNDS=10

# Frontend URL (for CORS) - Your production frontend URL
FRONTEND_URL=https://your-app.vercel.app,https://your-app.netlify.app

# Optional: Email service (for future features)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASSWORD=your-app-password
