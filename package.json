{
  "name": "root-proxy",
  "private": true,
  "scripts": {
    "build": "echo \"No root build step; site is static in repository root\"",
    "start": "echo \"Start not required at repo root\""
  },
  "engines": {
    "node": ">=16"
  }
}
