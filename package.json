{
  "name": "root-proxy",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -i ./css/tailwind.input.css -o ./css/tailwind.css --minify",
    "build": "npm run build:css && echo \"Static frontend and Vercel functions are ready\"",
    "start": "vercel dev",
    "seed-admin": "node --env-file=.env.local scripts/seed-admin.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "firebase-admin": "^13.5.0",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17",
    "vercel": "^59.16.0"
  }
}
